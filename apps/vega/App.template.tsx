import React from 'react'
import { Root } from '@described/shared-ui'
// Vega entry. Sizes are px at 1080p → scale 1.
export default function App() { return <Root apiBaseUrl={'https://api.described.example'} scale={1} /> }
